package start.students.core.application.dtos.validators;

/**
 * Validador de CPF
 * Valida formato e algoritmo de dígitos verificadores
 * 
 * NOTA: Este arquivo foi desativado. A validação de CPF agora ocorre
 * no Value Object CPF do domínio através de lógica de negócio pura.
 */
@Deprecated(forRemoval = true)
public @interface ValidCPF {
    // Mantido apenas para compatibilidade - será removido em breve
}
